specVersion: "v1beta"
serviceId: "quality-details-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quality-details-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
